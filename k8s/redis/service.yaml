apiVersion: v1
kind: Service
metadata:
  name: wealthgenie-redis
  namespace: wealthgenie
  labels:
    app: wealthgenie-redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  selector:
    app: wealthgenie-redis
