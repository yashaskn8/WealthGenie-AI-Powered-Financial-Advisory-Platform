apiVersion: v1
kind: Service
metadata:
  name: wealthgenie-mongodb
  namespace: wealthgenie
  labels:
    app: wealthgenie-mongodb
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
      name: mongodb
  selector:
    app: wealthgenie-mongodb
